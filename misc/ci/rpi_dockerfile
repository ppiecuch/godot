FROM sdthirlwall/raspberry-pi-cross-compiler

RUN apt-get update \
	&& install-debian scons \                 # Install some native build-time tools
	&& install-raspbian libpng-dev libz-dev \ # Install raspbian development libraries
	&& apt-get autoclean
