#ifndef TILED_SURFACE_PRIVATE_H
#define TILED_SURFACE_PRIVATE_H

void render_dab_mask(uint16_t *mask,
		float x, float y,
		float radius,
		float hardness,
		float aspect_ratio, float angle);

#endif // TILED_SURFACE_PRIVATE_H
